apiVersion: v1
kind: Service
metadata:
  name: {{ service_name }}
  namespace: {{ namespace }}
spec:
  {% if stateful %}
  clusterIP: None
  {% endif %}
  selector:
    app: {{ service_name }}
  ports:
    - port: {{ port }}
      targetPort: {{ port }}
